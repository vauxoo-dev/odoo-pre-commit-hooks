[tool.fixit]
enable = [
    "checks_odoo_module_fixit",
]
disable = ["fixit.rules"]
# formatter = "ufmt"
# disable = ["fixit.rules"]

# [[tool.fixit.overrides]]
# path = "examples"
# enable = ["checks_odoo_module_fixit"]
